import { Metric, Shell } from '@/components/Shell';
const metrics=['Total clicks','Unique visitors','Enquiries','Calls booked','Quotes sent','Clients won','Commission pending','Paid commission'];
export default function Page(){return <Shell><div className="glass rounded-[2rem] p-8"><span className="badge">Approved affiliate dashboard</span><h1 className="mt-4 text-4xl font-black">Referral overview</h1><p className="mt-3 max-w-3xl text-slate-300">Pending applicants only see application status; approved affiliates see privacy-safe metrics for their own links, leads and commissions.</p><div className="mt-7 grid gap-4 md:grid-cols-4">{metrics.map(m=><Metric key={m} label={m} value={m.includes('Commission')?'R0':'0'}/>)}</div><section className="card mt-6"><h2 className="text-xl font-bold">Application Under Review / Rejected states</h2><p className="mt-2 text-slate-300">Backend status gates render either the review placeholder, rejection reason with 48-hour countdown, or this approved dashboard shell.</p></section></div></Shell>}
