import { Shell } from '@/components/Shell';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;return <Shell nav="admin"><section className="glass rounded-[2rem] p-8"><span className="badge">Applicant {id}</span><h1 className="mt-4 text-4xl font-black">Review application</h1><p className="mt-3 text-slate-300">Inspect submitted details, links, timeline and internal notes. Approve creates affiliate profile/code; decline requires a reason and schedules 48-hour deletion.</p><div className="mt-6 flex gap-3"><form action={`/api/admin/applications/${id}/approve`} method="post"><input className="input mb-3" name="message" placeholder="Optional approval message"/><button className="btn btn-primary">Approve</button></form><form action={`/api/admin/applications/${id}/decline`} method="post"><input className="input mb-3" name="reason" placeholder="Required decline reason" required/><button className="btn btn-muted">Decline</button></form></div></section></Shell>}
